version: 2

models:
  - name: int_order_details
    description: "Table with a unique row for each order, containing attributes about that order enriched with summary measures/data."
    columns: 
      - name: conversion rate